# 阶段 2：多角度辩证讨论

本阶段目标：对六个关键议题各比较至少三种方案，从架构、构建、平台、QA 与 UX 视角给出可回滚决策，并显式回应反方观点。

## 1. mpv 功能识别抽象

| 方案 | 架构/平台视角 | 构建/QA/UX 视角 | 结论 |
|---|---|---|---|
| A. 抽象 `MpvBackend` | 统一生命周期、属性、观察与 IPC；允许 legacy adapter | 易 mock、调用点少；不会让 UI 判断平台 | **采用** |
| B. capability 字典 | 灵活、无需继承 | 容易出现字符串键漂移，调用方仍需分支 | 仅作为运行时探测输出，不作为控制面 |
| C. 插件注册表 | 第三方扩展能力最强 | 当前项目规模下增加装载、版本与安全面 | 暂不采用 |

推荐：新增 `MpvBackend`，由 `LegacyModuleMpvBackend` 包装现有 Windows/Linux/macOS 模块。  
反方观点：“已有 `MediaBackend`，再加接口重复。”回应：`MediaBackend` 只表达媒体类型服务；暂停、property observe、raw IPC 是播放器控制域，混入通用接口会污染 HTML 后端。

## 2. 原生 libmpv bundle 策略

| 方案 | 优点 | 缺点 |
|---|---|---|
| A. `src/bin/<platform>/` 自带经验证运行时 | 可复现、用户开箱即用、ABI 固定 | 包体积与安全更新责任上升 |
| B. 仅系统 libmpv | 包小、跟随发行版安全更新 | 发行版路径/版本差异，首启失败率高 |
| C. 自带 + 系统混合 | 兼顾可用性与高级用户控制 | 选择规则和诊断更复杂 |

推荐：**构建时显式选择的混合策略**，发布版默认自带已验证 payload，开发/发行版构建可选 system；不要在同一运行中“静默优先系统”，以免不可复现。  
反方观点：“系统库总是更安全。”回应：安全更新更快不等于兼容性可预测；必须同时记录版本、探测结果与回退原因，发布方也需更新 bundle。

## 3. KDE Wayland 视频壁纸

| 方案 | 平台可行性 | 性能/UX | 风险 |
|---|---|---|---|
| A. mpvpaper layer-shell | KWin 支持 layer-shell；项目已有 mpvpaper 路径 | 独立进程、复用 mpv IPC，最小改动 | 外部工具质量与多屏行为需真机验证 |
| B. KWin scripted effect | 与 compositor 深度集成 | 理论上层级最稳 | KWin scripting/effect 不是稳定的视频壁纸媒体 API，维护成本高 |
| C. Plasma wallpaper plugin | 最符合 Plasma 模型 | 可做每屏配置与系统设置集成 | 需 C++/QML 插件、独立发布与 Plasma API 适配 |

推荐：短期采用 A，并把 KDE 标为 best-effort；中长期若维护者愿意承担 Plasma 专用组件，再做 C。  
反方观点：“KWin effect 更原生。”回应：原生不等于合适；把解码和媒体生命周期塞进 effect 会放大 compositor 崩溃与帧压力，且偏离当前 Python 架构。

## 4. JSON i18n 运行时切换

| 方案 | UX | 工程成本 | 兼容性 |
|---|---|---|---|
| A. JSON 语言事件 + 可见 UI 主动重建 | 即时、统一 | 中等；需避免重复连接 | **采用**，复用既有重建函数 |
| B. 人工发送 `QEvent::LanguageChange` | 可复用 Qt 事件语义 | 控件仍需自行从 `t()` 取文案，易误以为 QTranslator 生效 | 不采用 |
| C. 提示重启 | 最简单 | UX 差，难验证资源是否加载 | 仅作为极端降级提示 |

推荐：A，保留 `t()` 与 JSON 字典，不引入 QTranslator。  
反方观点：“重建整个界面代价大。”回应：语言切换是低频操作，现有代码已实现重建；事件只消除入口分裂，后续可逐步优化为局部 refresh。

## 5. i18n 打包坑根治

| 方案 | 防错位置 | 向后兼容 | 结论 |
|---|---|---|---|
| A. freeze 前断言 `.json` 不是 gzip | 最早失败，构建日志清晰 | 不修复已发布错误包 | **采用** |
| B. loader magic-byte 探测 | 用户端自恢复 | 会掩盖构建错误 | 保留为旧包兼容 |
| C. 改 `.json.gz` | 格式显式 | 需迁移路径和所有构建配置，当前资源很小 | 暂不采用 |

推荐：A+B 双层防线。  
反方观点：“既然 loader 能读 gzip，断言多余。”回应：容错不能替代供应链一致性；错误后缀会影响审计、签名、第三方工具和其他语言文件。

## 6. KDE Wayland 全局热键

| 方案 | 安全/权限 | 可移植性 | 结论 |
|---|---|---|---|
| A. XDG GlobalShortcuts Portal | compositor 授权，只交付注册动作 | KDE 5.27+ 已公开支持；其他桌面由 backend 决定 | **采用** |
| B. `pynput` | 现有 X11 路径成熟 | Wayland 通常不能安全获取全局输入 | 保留 X11 fallback |
| C. 直调 KWin/KGlobalAccel | KDE 专用且可进入系统快捷键 | API/应用身份与清理语义耦合，跨桌面为零 | 仅作为未来 KDE 插件路线 |

推荐：Wayland 使用 Portal，X11 使用 pynput。Portal 注册在独立 asyncio 线程中，避免阻塞 Qt；用户拒绝或 backend 缺失时返回失败并保持功能可降级。  
反方观点：“Portal 会弹授权框，体验不如静默注册。”回应：这是 Wayland 安全模型的一部分；静默全局键盘钩子本身就是应避免的能力。

## 综合决策

> 采用“最小兼容适配 + 显式能力探测 + 构建期早失败 + 真机可复现”的路线。所有平台专用实现仍由原模块拥有，新增抽象只收敛控制面；未知或未验证的 Wayland 行为一律标为 best-effort，而非宣称完全支持。

本阶段产出：六项方案均完成三选一论证、反方回应与可回滚推荐。
